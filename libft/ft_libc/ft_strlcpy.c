//
// Created by camir on 8/31/2021.
//

#include "ft_strlcpy.h"

size_t ft_strlcpy(char * dst, const char * src, size_t size) // copy src into dest
{
    int srcLength = ft_strlen(src);
    int destLength = ft_strlen(dst);
    // do it BACKWARDS
    int counter = 0;
    return srcLength;       // Method must always return the src string length.
}